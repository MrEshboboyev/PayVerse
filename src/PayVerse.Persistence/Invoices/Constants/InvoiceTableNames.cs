namespace PayVerse.Persistence.Invoices.Constants;

internal sealed class InvoiceTableNames
{
    internal const string Invoices = nameof(Invoices);
    internal const string InvoiceItems = nameof(InvoiceItems);
}