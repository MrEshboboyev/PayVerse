namespace PayVerse.Persistence.VirtualAccounts.Constants;

internal sealed class VirtualAccountTableNames
{
    internal const string VirtualAccounts = nameof(VirtualAccounts);
    internal const string Transactions = nameof(Transactions);
}