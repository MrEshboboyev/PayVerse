namespace PayVerse.Persistence.Wallets.Constants;

internal sealed class WalletTableNames
{
    internal const string Wallets = nameof(Wallets);
    internal const string WalletTransactions = nameof(WalletTransactions);
}